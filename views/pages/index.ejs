<% include ../partials/header.ejs %>
<div id="clickBait" ><!--data-spy="affix" data-offset-top="130"-->
  <ul class="nav nav-pills nav-justified link1" >
    <li class="active"><a href="#">Home</a></li>
    <li><a href="/people" class="link">The People</a></li>
    <li><a href="/robots" class="link">The Robot</a></li>
    <li><a href="/club" class="link">The Club</a></li>
  </ul>
</div>
<div class="slideshow-container">
    <!-- Full-width images with number and caption text -->
    <div id="slideshow">

    </div>
    <!-- Next and previous buttons -->
    <a class="prev" onclick="plusSlides(-1)">&#10094;</a>
    <a class="next" onclick="plusSlides(1)">&#10095;</a>
</div>
<br>

<!-- The dots/circles -->
<div style="text-align:center">
    <div id="dots"></div>
    <span class="dot" onclick="currentSlide(1)"></span>
    <span class="dot" onclick="currentSlide(2)"></span>
    <span class="dot" onclick="currentSlide(3)"></span>
</div>
<% include ../partials/footer.ejs %>
<script>
    var xhttp = new XMLHttpRequest();
    xhttp.onreadystatechange = function(){
        if(this.readyState == 4 && this.status == 200){
            myFunction(this);
        }
    };
    xhttp.open("GET", "/images", true);
    xhttp.send();
    function myFunction(xml) {
        var xmlDoc = xml.responseXML;
        var date = xmlDoc.getElementsByTagName("date");
        var caption = xmlDoc.getElementsByTagName("caption");
        var image = xmlDoc.getElementsByTagName("url");
        var articleNum = 0;
        for(i = 0; i < date.length; i++){
            if(b[i].childNodes[0].nodeValue == category || category == ""){
                articleNum += 1;
                document.getElementById("slideshow").innerHTML += "<div class='mySlides fade'><div class='numbertext'>"+articleNum+" / "+ date.length +"</div><img src='"+ image[i].childNodes[0].nodeValue +"' style='width:100%'><div class='text'>" + caption[i].childNodes[0].nodeValue +"</div></div>";
            }
        }
    }
</script>
</body>
</html>